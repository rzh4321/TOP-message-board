extends layout

block content
  h1= title
  p Welcome to #{title}

  a(href='/new') Add New Message
  ul 
    each message in messages
      li
        p User: #{message.user}
        p Date: #{message.added}
        p #{message.text}

